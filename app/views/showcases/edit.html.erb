<%= collection_nav(@showcase.exhibit.collection, :showcases) %>

<%= back_action_bar(exhibit_showcases_path(@showcase.exhibit), '#' ) %>

<%= react_component 'ShowcaseEditor', {
          itemsJSONPath: @showcase.exhibit.items_json_url,
          sectionsJSONPath: showcase_sections_path(@showcase, :json) },
         {prerender: false }
%>
