<% page_title(t('.title')) %>
<%= collection_nav(@item.collection, :items) %>

<%= simple_form_for [@item.collection, @item], :html => { class: 'new-item-dropzone', :multipart => true, :id => "fileupload"  } do |f| %>
  <%= render partial: 'new_form', locals: { f: f, title: t('.title') } %>
<% end %>

