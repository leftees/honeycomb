<div class="page-header">
  <h1><%=t '.title', :default => "Upload New Items" %></h1>
</div>
<div class="container">
  <div class="row">
    <%= link_to 'Back to Collection', collection_items_path(@item.collection), class: 'btn btn-large' %>
  </div>
  <div class="row">
  <%= form_for [@item.collection, @item], :html => { class: 'dropzone', :multipart => true, :id => "fileupload"  } do |f| %>
    <div class="fallback">
      <%= f.file_field :image %>
    </div>
  <% end %>
  </div>
</div>
