<% if @item.parent %>
  <%= item_title(@item.parent) %>
<% else %>
  <%= item_title(@item) %>
<% end %>

<%= render partial: "show", locals: { item: @item } %>


<%= DeleteSection.new(@item).display do | ds |
  ds.name = "Item"
  ds.path = [@item.collection, @item]
end %>
