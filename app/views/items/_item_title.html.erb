<% if item.is_parent? %>
  <h2><%= link_to item.title, item.show_path %></h2>
<% else %>
  <h2><%= link_to item.parent.title, collection_item_path(@item.parent.collection, @item.parent) %> / <%= link_to item.title, item.show_path %></h2>
<% end %>
