<table class="table table-striped datatable item-list">
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th>Item</th>
      <th>Last Modified</th>
      <th>Last Modified Timestamp</th>
      <th>Sortable Title</th>
      <th>Image Name</th>
    </tr>
  </thead>
  <tbody>
    <% items.each do |item| %>
      <tr>
        <td>
          <input type="checkbox" class="show-selected-menu" />
        </td>
        <td>
          <%= link_to item.react_thumbnail, item.show_path %>
        </td>
        <td>
          <h4 class="media-heading"><%= h link_to item.title, item.show_path %></h4>
          <%= h item.description %>
        </td>
        <td>
          <%= item.updated_at.to_s(:long) %>
        </td>
        <td>
          <%= item.updated_at.to_i %>
        </td>
        <td>
          <%= item.sortable_title %>
        </td>
        <td>
          <%= item.image_title %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
