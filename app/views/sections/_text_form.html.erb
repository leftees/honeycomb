<%= display_errors(f.object) %>

<%= f.input :title %>

<%= f.input :description, input_html: { class: 'honeycomb_redactor' }, as: :text %>

<%= f.hidden_field :display_type, value: 'text'%>
<%= f.hidden_field :order %>

