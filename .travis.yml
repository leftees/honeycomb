language: ruby
cache: bundler
sudo: false
rvm:
  - 2.1.4
script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate test
  - bundle exec rspec spec
before_script:
  - mysql -e 'create database item_admin_test'
  - npm install
cache:
  bundler: true
  directories:
  - node_modules
